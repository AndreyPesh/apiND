export const enum NameKeyPrivateToken {
  accessTokenPrivate = 'accessTokenPrivateKey',
  refreshTokenPrivate = 'refreshTokenPrivateKey'
}

export const enum NameKeyPublicToken {
  accessTokenPublic = 'accessTokenPublicKey',
  refreshTokenPublic = 'refreshTokenPublicKey'
}
